#include <stdio.h>
#include <math.h>

int main() {
    int x, y, n, m;
    scanf("%d %d", &x, &y);
    n = x-x;
        switch (x-x)+((-x)-x){
            case 0: printf("+\n"); break;
            default: printf("-\n"); break;


        }
//    n = (x%10)/(x%10);
//    n = pow(1, x);
    m = pow(1, y);
    printf("%d %d\n", n, m);

return 0;
}